
# Ejercicio 1: define una variable de nombre `a` y cuyo valor sea -1
a <- -1

# Ejercicio 2: añade debajo otra línea para definir una variable `b` con el valor 5.
# Tras ello múltiplica ambas variables
b <- 5
a * b # sin guardar
multiplicacion <- a * b # guardado
multiplicacion

# Ejercicio 3: modifica el código inferior para definir dos
# variables c y d, con valores 3 y -1. Tras ello divide las variables.
c <- 3
d <- -1
c / d # sin guardar
division <- c / d # guardado
division

# Ejercicio 4: asigna un valor positivo a `x` y calcula su raíz cuadrada;
# asigna otro negativo `y` y calcula su valor absoluto con la función `abs()`.
x <- 5
sqrt(x)
y <- -2
abs(y)

# Ejercicio 5: usando la variable `x` ya definida, completa/modifica
# el código inferior para guardar en una nueva variable `z` el
# resultado guardado en `x` menos 5.
z <- x - 5
z
